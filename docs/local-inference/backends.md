# Backends and acceleration on Android

An honest account of what can and cannot accelerate GGUF inference on an Android
phone. There is a lot of confident wrong information about this, most of it
generated by people who assume that because a device has an NPU, a local LLM can
use it.

## Summary

| Backend | Can execute GGUF? | Status here |
| --- | --- | --- |
| ggml CPU (ARM NEON + dotprod/i8mm/fp16) | Yes | **The shipping path.** |
| Vulkan | Yes, upstream | Real but caveated. Not built today. |
| OpenCL (Adreno) | Yes, upstream | Real but narrow. Not built today. |
| NNAPI | **No** | Impossible — no format bridge. Also deprecated. |
| LiteRT / TFLite delegates (GPU, NNAPI, QNN, Core ML) | **No** | Impossible — different model format entirely. |

## The CPU path

This is what OllamaMobile ships, and on a phone it is not the consolation prize.
Transformer decoding at batch size 1 is dominated by memory bandwidth, not by
arithmetic throughput. A phone's CPU cluster and its GPU sit behind the same
LPDDR controller, so the theoretical FLOPS advantage of the GPU converts into far
less real speed-up than it does on a discrete-GPU desktop.

The CPU backend gets its performance from ARMv8 extensions:

* **`dotprod`** (FEAT_DotProd, ARMv8.2-A) — `SDOT`/`UDOT` compute four 8-bit
  multiply-accumulates per lane per instruction. This is the single most
  important extension for quantised inference and is present on essentially
  every 64-bit Android SoC from roughly 2019 onward.
* **`i8mm`** (FEAT_I8MM, ARMv8.6-A) — `SMMLA` performs a small 8-bit integer
  matrix multiply per instruction, a further step up for the matmul-shaped work
  in prompt processing. Present on newer cores; absent on plenty of devices
  still in use.
* **`fp16`** (FEAT_FP16) — native half-precision vector arithmetic, used for
  `F16` tensors and for the scale arithmetic inside quantised kernels.
* **SVE / SVE2** — present on some cores. ggml has SVE paths; whether they win
  over the NEON paths is device-dependent and we have measured nothing.

### Multi-variant dispatch

Because these extensions are not universal, the build produces the ggml CPU
backend several times over — one shared object per feature tier — and selects at
runtime. `GGML_CPU_ALL_VARIANTS=ON` produces the variants,
`GGML_BACKEND_DL=ON` makes them `dlopen`-able, and ggml's loader probes the CPU
and picks the best supported one. The mechanics are on the
[native build page](native-build.md).

The alternative — a single binary compiled for one feature level — forces a
choice between leaving a lot of throughput unused on modern hardware and
crashing with `SIGILL` on older hardware. Neither is tenable for a universal APK
distributed through GitHub Releases.

## Vulkan

llama.cpp has a genuine, actively maintained Vulkan backend, and it does run on
Android. It is not vapourware. The caveats are what keep it off the shipping
path for now:

* **Driver quality is the whole story.** Mali, Adreno, Xclipse and PowerVR
  drivers differ wildly in which Vulkan features they expose and how correct
  they are. Support for cooperative matrix extensions (`VK_KHR_cooperative_matrix`
  and friends), which is where most of the Vulkan speed-up comes from, is
  patchy on mobile.
* **Shader compilation cost.** The backend compiles a large number of pipelines
  at initialisation. On mobile this is a visible startup delay, and the pipeline
  cache has to be persisted and invalidated correctly to avoid paying it every
  launch.
* **Allocation limits.** `maxMemoryAllocationSize` and
  `maxStorageBufferRange` on mobile drivers are frequently far below what a
  multi-gigabyte model wants in a single allocation, forcing tensor splitting.
* **A GPU-crashed driver takes the process with it.** A bad shader dispatch is
  not a catchable exception; it is a device-lost or a GPU hang. This is what
  motivates the crash quarantine described below.

If Vulkan is enabled later it will be as an opt-in, per-device, quarantined
backend — never a default. It is not built today.

## OpenCL

llama.cpp also has an OpenCL backend, developed with Qualcomm and targeting
Adreno GPUs specifically. On the right Snapdragon device with the right
`cl_qcom_*` extensions it works, and it has a narrower quantisation coverage
than the CPU path (it has historically focused on `Q4_0` and float types).

It is Adreno-first by construction. On a Mali or Xclipse device you are relying
on a generic OpenCL path, if the vendor ships an OpenCL driver at all — many do
not expose one to apps. Combined with the same driver-crash exposure as Vulkan,
this makes it a per-device opt-in at best. It is not built today.

## NNAPI and LiteRT: not applicable, at all

!!! danger "NNAPI and LiteRT/TFLite delegates cannot run GGUF"
    This is not a matter of effort or optimisation. There is **no format
    bridge**. GGUF is a container of quantised tensors plus metadata designed
    for ggml's own graph executor. NNAPI consumes an operation graph built
    through its C API; LiteRT consumes a `.tflite` FlatBuffer. Neither can be
    handed a GGUF file, and neither implements the quantisation schemes
    (k-quants, i-quants, `Q4_0` block layouts) that the weights are stored in.

    Converting a model would mean dequantising it, re-exporting the graph into
    the other framework's format, and re-quantising into a scheme that framework
    supports. That is a different model artefact, produced offline, with
    different weights and different quality — not "running your GGUF on the
    NPU".

    NNAPI is additionally **deprecated**: Google deprecated it in Android 15 and
    directs new work to LiteRT. Building on it now would be building on a
    removed API. Vendor NPU access on Android today means vendor SDKs (Qualcomm
    QNN, MediaTek NeuroPilot, Samsung ENN) with their own model formats, their
    own toolchains, and no cross-device story.

If someone tells you the phone's NPU is idle while the model runs, they are
correct, and there is nothing OllamaMobile can do about it without shipping a
completely different model artefact per SoC vendor.

## So what does `:core-ml` do?

The module name invites exactly the misunderstanding above, so it is worth
being blunt: **`:core-ml` is not an inference accelerator.** It contains no
NNAPI, no LiteRT, no delegate, and no path by which a model executes anywhere
other than ggml. Its build file says so in a comment, and `checkModuleGraph`
keeps its dependencies honest.

What it actually owns:

**CPU feature probing.** Reading `/proc/cpuinfo` and the `Build.SUPPORTED_ABIS`
/ `HWCAP` surface to determine which of dotprod, i8mm, fp16 and SVE the device
supports, plus core topology (how many big cores, how many little) and
`MemAvailable`. This is a plain capability record, cached per boot.

**The variant selection policy.** ggml will pick a CPU variant on its own, but we
need to know *which* it picked and be able to override it — to force a lower
tier on a device where the higher one has misbehaved, and to report the choice
in diagnostics and benchmark output. The policy maps a probed feature set to a
preferred variant and consults the quarantine ledger before committing.

**The backend crash quarantine ledger.** Native crashes are not catchable. If a
backend selection causes a `SIGILL`, a `SIGSEGV` or a GPU device-lost, the
process dies and the user relaunches into the same crash. The ledger breaks that
loop: the intended backend and variant are written to persistent storage
*before* the native library is loaded, and cleared once inference has
successfully produced tokens. A launch that finds an uncleared record knows the
previous attempt died, marks that combination as quarantined for the device, and
falls back to the next-safest option. This is the mechanism that would make an
opt-in Vulkan or OpenCL backend survivable.

**Thermal and performance policy.** Subscribing to `PowerManager`'s thermal
status, deciding when to reduce thread count or pause generation, and issuing
the appropriate performance hints. The tuning consequences are on
[tuning](tuning.md).

**An int8 vector kernel for RAG.** Brute-force cosine similarity over int8
embedding vectors with a per-vector scale, using NEON dot-product instructions.
This is genuinely local numeric work that has nothing to do with llama.cpp, and
it is the reason `:core-ml` exists as a module rather than as a package inside
`:core-llm` — RAG retrieval must work with `-Pollama.nativeSource=none`, when
there is no llama.cpp at all. See [hybrid search](../rag/hybrid-search.md).

!!! warning "Status"
    `:core-ml` is registered in the module graph with its dependencies and
    conventions in place, but the classes described above are not implemented
    yet. This page describes the intended contents of the module, not code you
    can read today.

## Related

* [Build flags](native-build.md) — how the CPU variants are produced.
* [Quantisation](quantization.md) — which formats which kernels can accelerate.
* [Tuning](tuning.md) — thread counts, batching, thermal behaviour.
