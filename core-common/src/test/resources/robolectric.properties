# Pinned rather than inherited from targetSdk. compileSdk is 37 and Robolectric
# only ships shadows for SDKs it has been released against, so letting it follow
# the compile level would break the build on every SDK bump before Robolectric
# catches up. Raise this deliberately, with a Robolectric bump.
sdk=34
